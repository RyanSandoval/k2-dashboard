# Gates: retire the pile by the group, not the row

OWNS: spec/task-retirement/**, index.html (the Task Retirement block only)

Scope: the three triage actions (Keep / Snooze / Delete) have shipped on every stale task
row for months, wired to `triageTask()` and K2Archive. Across all 883 tasks, `lastTriaged`
is set on **zero** of them and `hideUntil` on three. Nobody triages 697 things one at a
time — and 545 of those were written by the plan-files cron, not by Ryan. So this is not
a nicer version of the row actions. It is bulk retirement grouped by origin, with the
rows past the render cap included in "select all", one save for the batch, and one undo.

- [x] G1: the surface shows the right pile — stale, open, top-level, not already trashed, oldest first, losslessly grouped
  CHECK: node /Users/ryansandoval/k2-dashboard/spec/task-retirement/checks/g1_selection.mjs
  EXPECT: G1 PASS
  EVIDENCE: exit=0; shell=/bin/sh; cwd=/Users/ryansandoval/k2-dashboard/spec/task-retirement; path=7f6bac7bdd6a/13 entries; output=G1 PASS: 697 stale tasks in 8 groups, oldest first, lossless, largest is plan-sync at 545; snoozing removes and restores; 2 true-duplicate key(s) share a row, 0 conflated where id-only keying would have conflated 1

- [x] G2: selecting a group covers every row in it including those past the render cap, and retires them in a single save
  CHECK: node /Users/ryansandoval/k2-dashboard/spec/task-retirement/checks/g2_bulk.mjs
  EXPECT: G2 PASS
  EVIDENCE: exit=0; shell=/bin/sh; cwd=/Users/ryansandoval/k2-dashboard/spec/task-retirement; path=7f6bac7bdd6a/13 entries; output=G2 PASS: selecting "plan-sync" covered all 545 rows though only 15 render, retired them in 1 save, pile 697 -> 152, DATA.tasks length unchanged at 883, 0 hard-purged

- [x] G3: retirement is exactly reversible and is not regenerated by the plan-files cron
  CHECK: node /Users/ryansandoval/k2-dashboard/spec/task-retirement/checks/g3_undo.mjs
  EXPECT: G3 PASS
  EVIDENCE: exit=0; shell=/bin/sh; cwd=/Users/ryansandoval/k2-dashboard/spec/task-retirement; path=7f6bac7bdd6a/13 entries; output=G3 PASS: retiring 545 "plan-sync" tasks is fully reversible (0 rows drift after undo), rows stay in DATA.tasks, and the plan-sync cron still dedupes against that array so retirement is not regenerated

- [x] G4: the page is reachable from nav, both dispatch sites and the dashboard alert, and index.html parses
  CHECK: node /Users/ryansandoval/k2-dashboard/spec/task-retirement/checks/g4_wiring.mjs
  EXPECT: G4 PASS
  EVIDENCE: exit=0; shell=/bin/sh; cwd=/Users/ryansandoval/k2-dashboard/spec/task-retirement; path=7f6bac7bdd6a/13 entries; output=G4 PASS: page, nav entry, both dispatch sites, section expansion and the dashboard alert all point at task-retirement; no id-only lookups left; 7 script blocks parse

Notes:
- G1 carries a control: it asserts that keying selection on `id` alone would still
  conflate two different tasks in the live data. `DATA.tasks` is not id-unique — 3 ids
  cover 7 rows and one pair holds different text under the same id — so identity here is
  id + text. If that control ever stops finding a conflict, the guard is untested.
- G3 reads the plan-sync cron's own prompt from /tmp/plan-sync-cron.txt and asserts it
  still dedupes against the live tasks array. That is the only reason soft archive
  suppresses regeneration. Refresh with:
  openclaw cron get 35275f79-87ee-4c2c-90d6-02f7ba5cc1ad | python3 -c "import json,sys;print(json.load(sys.stdin)['payload']['message'])" > /tmp/plan-sync-cron.txt
- Gates need a live data snapshot at /tmp/k2data.json:
  gh api repos/RyanSandoval/k2-data/git/blobs/$(gh api repos/RyanSandoval/k2-data/contents/data.json --jq .sha) --jq .content | tr -d '\n' | base64 -d > /tmp/k2data.json
- Not built: Rewrite (inline text edit). A 697-item pile does not need a per-row editor,
  and the row actions already carry Keep and Snooze.
